export const FLOW_MANAGER = 'flowManager';
export const CURRENT_STEP = 'currentStep';
export const CURRENT_URL = 'currentUrl';
export const EXTRA_PROPS = 'extraProps';
// default path - assume that component has wrapped with withRouter before
export const DEFAULT_PATH = 'match.params.id';
export const DEFAULT_AGREEMENT_ID = -1;
export const DEFAULT_ID = -1;